<%= form_with model: @blog_post do |form| %>
  <% if form.object.errors.any? %>
    <div id="error_explanation">
      <h3 style="color: red; font-family: monospace;"><%= pluralize(form.object.errors.count, "error") %> prohibited this blog_post from being saved:</h2>

      <ul>
      <% form.object.errors.full_messages.each do |message| %>
        <li style="color: red; font-size: 1vw; font-family: monospace;"><%= message %></li>
      <% end %>
      </ul>
    </div>
    <% end %>
  <div>
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div>
  <%= form.label :cover_image %>
  <%= image_tag blog_post.cover_image.url if blog_post.cover_image.present? %>
  <%= form.file_field :cover_image %>
  </div>

  <div>
    <%= form.label :content %>
    <%= form.rich_text_area :content %>
  </div>

  <div>
    <%= form.label :published_at %>
    <%= form.datetime_select :published_at, include_blank: true %>
 </div>
 <%= form.button %>

  <% end %>
